# Requirements for Streamlit Cloud deployment
# Financial RAG Agent - Lightweight version

# Core dependencies
streamlit>=1.28.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# ML and embeddings - DISABLED for Streamlit Cloud (too heavy)
# Uses fallback keyword-based search instead of semantic embeddings
# torch>=2.0.0,<2.5.0
# sentence-transformers>=2.5.0
# faiss-cpu>=1.7.4
# transformers>=4.30.0

# LLM clients (optional - will work in mock mode without API keys)
openai>=1.0.0
anthropic>=0.7.0

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Utilities
httpx>=0.25.0
